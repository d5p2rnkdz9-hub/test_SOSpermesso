---
phase: 06-visual-identity-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/[locale]/layout.tsx
  - src/components/layout/StickyHeader.tsx
  - src/components/ui/button.tsx
  - src/components/tree/AnswerCard.tsx
  - src/app/[locale]/WelcomeContent.tsx
  - public/logo-full.png
autonomous: true
requirements: [CSS-01, CSS-03, CSS-04]

must_haves:
  truths:
    - "App background is warm off-white (#FAF9F5) instead of blue-gray"
    - "Primary buttons are gold (#FFD700) with dark brown text (#5D4E00) and pill shape"
    - "Header has white background, logo image linking to sospermesso.it, language selector, back button"
    - "Answer cards show gold background when selected, light gold hover when unselected"
    - "Focus rings are gold instead of blue"
    - "CTA link on outcome page uses gold gradient"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Gold/yellow design tokens replacing blue palette"
      contains: "--primary.*48 100% 50%"
    - path: "public/logo-full.png"
      provides: "SOSpermesso logo image for header"
    - path: "src/components/layout/StickyHeader.tsx"
      provides: "White header with logo image"
      contains: "logo-full.png"
    - path: "src/components/ui/button.tsx"
      provides: "Pill-shaped gold buttons"
      contains: "rounded-full"
    - path: "src/components/tree/AnswerCard.tsx"
      provides: "Gold-themed answer card selection"
  key_links:
    - from: "src/app/globals.css"
      to: "all components via Tailwind theme"
      via: "CSS custom properties"
      pattern: "--primary"
    - from: "src/components/layout/StickyHeader.tsx"
      to: "public/logo-full.png"
      via: "Next.js Image or img tag"
      pattern: "logo-full"
---

<objective>
Restyle the app from blue palette to sospermesso.it gold/yellow visual identity: swap all CSS design tokens to gold/amber tones, replace the blue header with a white header containing the SOSpermesso logo image, and update buttons and answer cards to use gold styling with pill shapes.

Purpose: Make the questionnaire app visually match sospermesso.it so it feels like the same product, not a separate tool.
Output: Gold color palette across entire app, white logo header, gold pill buttons, gold answer card selection states.
</objective>

<execution_context>
@/Users/albertopasquero/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albertopasquero/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-visual-identity-restyle/06-CONTEXT.md

<interfaces>
<!-- Current CSS tokens in globals.css :root (lines 37-58) -->
:root {
  --background: 210 40% 96%;      /* blue-gray → change to warm off-white */
  --foreground: 215 25% 20%;
  --card: 0 0% 100%;
  --primary: 210 70% 45%;         /* blue → change to gold */
  --primary-foreground: 0 0% 100%; /* white → change to dark brown */
  --secondary: 210 20% 92%;       /* blue-tinted → change to gold-tinted */
  --muted: 210 15% 90%;           /* blue-tinted → change to gold-tinted */
  --accent: 210 60% 50%;          /* blue → change to gold */
  --border: 210 15% 85%;          /* blue-tinted → change to warm gray */
  --ring: 210 70% 45%;            /* blue → change to gold */
}

<!-- Sito_Nuovo reference values (from Sito_Nuovo/src/styles/main.css) -->
--taxi-yellow: #FFD700;
--taxi-yellow-light: #FFF176;
--taxi-yellow-dark: #FFC107;
Background: #FAF9F5 (warm off-white)
Button gradient: linear-gradient(135deg, #FFF9C4 0%, #FFD700 100%)
Button text: #5D4E00 (dark brown)
Button border: 1.5px solid #FFC107
Button radius: 9999px (pill)
Button hover: translateY(-2px) + enhanced shadow
Logo: Sito_Nuovo/IMAGES/logo-full.png

<!-- StickyHeader.tsx current structure -->
<header className="sticky top-0 z-50 w-full border-b border-primary/20 bg-primary">
  <!-- Currently: blue bg, text logo "SOSpermesso" -->
  <!-- Target: white bg, logo image, subtle border/shadow -->

<!-- button.tsx variants -->
buttonVariants default: "bg-primary text-primary-foreground hover:bg-primary/90"
rounded-md → needs rounded-full for pill shape

<!-- AnswerCard.tsx selected state -->
selected: 'border-primary bg-primary text-primary-foreground'
<!-- Target: gold bg, dark brown text when selected; light gold hover when unselected -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap color tokens to gold palette, add logo, restyle header</name>
  <files>src/app/globals.css, public/logo-full.png, src/components/layout/StickyHeader.tsx, src/app/[locale]/layout.tsx</files>
  <action>
**1. Copy logo file:**
Copy `../Sito_Nuovo/IMAGES/logo-full.png` to `public/logo-full.png`.

**2. Swap CSS design tokens in `src/app/globals.css`:**

Replace the `:root` block with gold/amber palette values. Convert hex to HSL for shadcn compatibility:

```
--background: 45 33% 97%;         /* #FAF9F5 warm off-white */
--foreground: 40 15% 20%;         /* warm dark, not cool blue */
--card: 0 0% 100%;                /* keep white */
--card-foreground: 40 15% 20%;    /* warm dark */
--popover: 0 0% 100%;             /* keep white */
--popover-foreground: 40 15% 20%;
--primary: 48 100% 50%;           /* #FFD700 taxi-yellow */
--primary-foreground: 42 100% 18%; /* #5D4E00 dark brown */
--secondary: 45 30% 92%;          /* warm light gray-gold */
--secondary-foreground: 40 15% 20%;
--muted: 45 15% 90%;              /* warm muted */
--muted-foreground: 40 10% 45%;   /* warm gray text */
--accent: 45 100% 42%;            /* #FFC107 amber/gold-dark */
--accent-foreground: 42 100% 18%; /* dark brown */
--destructive: 0 84% 60%;         /* keep red */
--destructive-foreground: 0 0% 100%;
--border: 40 15% 85%;             /* warm gray border */
--input: 40 15% 85%;              /* warm gray input border */
--ring: 48 100% 50%;              /* gold focus ring */
--radius: 0.75rem;                /* keep 12px */
```

Also update the `:focus-visible` rule (line 27) from `hsl(210 70% 45%)` to `hsl(48 100% 50%)` to match gold ring.

**3. Restyle StickyHeader.tsx:**

Replace the current blue header with a white header containing the logo image:

- Change `bg-primary` to `bg-white` (header background)
- Change `border-primary/20` to `border-black/8` (subtle border)
- Replace the text `<span>SOSpermesso</span>` with an `<img>` tag (or Next.js `Image`):
  - src="/logo-full.png"
  - alt="SOSpermesso"
  - Height: `h-[36px]` on mobile (sufficient for header height 56px)
  - Wrap in `<a href="https://www.sospermesso.it" target="_blank" rel="noopener noreferrer">` so logo links to main site
  - Add hover effect: `hover:scale-[1.02] transition-transform`
- Update BackButton and LanguageSelector: since header is now white bg, components need dark text colors. Change BackButton to use `text-foreground` (dark). Change LanguageSelector to use `text-foreground` with `border-border`.
- Add `shadow-sm` class to the header element for subtle elevation (per context: shadow `0 2px 12px rgba(0,0,0,0.08)` on scroll -- for now just use static `shadow-sm`)

**4. Update layout.tsx import (if needed):**
No changes expected -- StickyHeader is already imported and rendered. Just verify the import path is correct after header changes.

**Important notes:**
- Use CSS logical properties throughout (ms-/me-/ps-/pe-/text-start)
- Do NOT touch LawyerBanner.tsx -- its green-100/orange-100 colors are intentionally hardcoded
- StickyHeader is a server component (no 'use client') -- use a plain `<img>` tag, not Next.js `Image` (which needs client context for optimization). Or add 'use client' if using Image.
  </action>
  <verify>
    <automated>cd /Users/albertopasquero/Desktop/TECH/SOSpermesso/app && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- globals.css has gold palette tokens (--primary: 48 100% 50%)
- public/logo-full.png exists
- StickyHeader shows white bg with logo image linking to sospermesso.it
- Focus ring color is gold
- Build passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle buttons, answer cards, and welcome page interactive elements</name>
  <files>src/components/ui/button.tsx, src/components/tree/AnswerCard.tsx, src/app/[locale]/WelcomeContent.tsx, src/components/outcome/OutcomePage.tsx</files>
  <action>
**1. Restyle button.tsx for pill shape and gold gradient:**

In `src/components/ui/button.tsx`, update the `buttonVariants` cva:

- Change base classes: replace `rounded-md` with `rounded-full` (pill shape, 9999px)
- Update default variant: change from plain `bg-primary text-primary-foreground hover:bg-primary/90` to a gold gradient style. Since Tailwind doesn't have built-in gradient utilities that match `linear-gradient(135deg, #FFF9C4 0%, #FFD700 100%)`, add a custom approach:
  - Use `bg-primary text-primary-foreground` (which now maps to gold/#5D4E00 via token swap) as the base
  - Add `border border-amber-500` for the 1.5px gold border effect (or `border-[1.5px] border-[#FFC107]`)
  - Add hover: `hover:shadow-md hover:-translate-y-0.5` for the Sito_Nuovo lift effect
  - Add active: `active:scale-[0.98]` for press feedback
- Update size variants: keep h-12/h-14 for touch targets, just update radius references from `rounded-md` to `rounded-full`

**2. Restyle AnswerCard.tsx:**

In `src/components/tree/AnswerCard.tsx`:

- Selected state: change from `border-primary bg-primary text-primary-foreground` to `border-primary bg-primary text-primary-foreground` -- this auto-updates to gold via token swap. Verify the gold bg + dark brown text looks correct.
- Unselected hover: change `hover:bg-secondary` to `hover:bg-[#FFF9C4]/50` (light gold tint on hover, per context decision "light gold tint on hover")
- Keep the existing border-2, rounded-xl, min-h-[48px] touch targets
- Keep active:scale-[0.98]

**3. Update WelcomeContent.tsx CTA button and input:**

In `src/app/[locale]/WelcomeContent.tsx`:

- Input focus ring: already uses `focus:ring-primary` which will auto-update to gold. Verify.
- The `<Button>` component already uses primary variant which will auto-update.
- No changes expected if token swap handles it. But check the input `border-border` which should now be warm gray.

**4. Update OutcomePage.tsx CTA link:**

In `src/components/outcome/OutcomePage.tsx`:

- The "Read full guide" CTA link (line 86-91) uses `bg-primary text-primary-foreground` inline classes -- these auto-update via token swap.
- Change `rounded-xl` to `rounded-full` on this CTA to match pill button style (per context: button border-radius is pill 9999px).
- Add hover effect: `hover:shadow-md hover:-translate-y-0.5 transition-all` to match Sito_Nuovo button behavior.
- The restart button below can keep its outline style but should also get `rounded-full`.

**Important notes:**
- Do NOT add Poppins font (discretion decision: Inter is sufficient, saves ~30KB font load)
- Answer cards keep rounded-xl (not pill) -- pill shape is only for CTA buttons
- Verify the gold-on-gold doesn't create contrast issues
  </action>
  <verify>
    <automated>cd /Users/albertopasquero/Desktop/TECH/SOSpermesso/app && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
- button.tsx uses rounded-full (pill) with gold hover lift effect
- AnswerCard selected state shows gold bg + dark brown text
- AnswerCard hover shows light gold tint
- OutcomePage CTA link is pill-shaped with hover lift
- Build passes with no errors
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes with no errors
2. All primary-colored elements render as gold (#FFD700) not blue
3. Header is white with logo image, not blue with text
4. Buttons are pill-shaped (rounded-full)
5. Answer cards show gold selection state
6. Focus rings are gold when tabbing through interactive elements
7. LawyerBanner still shows green-100/orange-100 (not gold)
</verification>

<success_criteria>
- The entire app uses gold/amber color palette instead of blue
- Header shows SOSpermesso logo on white background
- All CTA buttons are pill-shaped with gold styling
- Answer cards during tree questions use gold visual language
- Build passes, no broken imports or missing files
</success_criteria>

<output>
After completion, create `.planning/phases/06-visual-identity-restyle/06-01-SUMMARY.md`
</output>
